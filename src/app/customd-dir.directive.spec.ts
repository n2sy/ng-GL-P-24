import { CustomdDirDirective } from './customd-dir.directive';

describe('CustomdDirDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomdDirDirective();
    expect(directive).toBeTruthy();
  });
});
